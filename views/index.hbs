<h1>{{title}}</h1>

{{#if user}}
<p>Welcome to Google oAuth <a href="{{user.url}}" target=_blank>{{user.displayName}}</a></p>
<h3>Here are some things that I know about you:</h3>
<ul>
  <li>You are {{user.gender}}</li>
  <li>And here's your picture: <img src="{{user.profileImageURL}}" alt="{{user.displayName}}'s picture'" /></li>
  <li>
    And here are a bunch of your pictures:
    <ul>
      {{#user.photos}}
        <li><img src="{{value}}" alt="a picture" /></li>
      {{/user.photos}}
    </ul>
  </li>
  <li>
    Yeah kinda creepy...
  </li>

</ul>
{{/if}}
